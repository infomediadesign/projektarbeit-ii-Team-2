#include "INCLUDES.h"
#include "Level/Level.h"
/*
#define NUM_FRAMES  3       // Number of frames (rectangles) for the button sprite texture

class Titlescreen
{
public:

  Game::Level *level;

  Texture2D button = LoadTexture("assets/graphics/button.png");

  float frameHeight = (float)button.height/NUM_FRAMES;
  Rectangle sourceRec = { 0, 0, (float)button.width, frameHeight };

  // Define button bounds on screen
  //Rectangle btnBounds = {  Game::ScreenHeight/2.0f - button.width/2.0f, Game::ScreenWidth/2.0f - button.height/NUM_FRAMES/2.0f, (float)button.width, frameHeight };
  Rectangle btnBounds = { Game::ScreenWidth/2.0f, Game::ScreenHeight/2.0f, (float)button.width, frameHeight };
  Rectangle buttonrec = {Game::ScreenHeight/2, Game::ScreenWidth/2, 60, 20};

  int btnState = 0;               // Button state: 0-NORMAL, 1-MOUSE_HOVER, 2-PRESSED
  bool btnAction = false;         // Button action should be activated

  Vector2 mousePoint = { 0.0f, 0.0f };

  void update();
  void draw();
};
*/

